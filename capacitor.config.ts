import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'helloworld_crud',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
